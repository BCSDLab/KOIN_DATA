version: 2

models:
  - name: stg_ga4_events
    description: Cleaned GA4 event stream with commonly used event parameters extracted.
    columns:
      - name: event_date
        tests:
          - not_null
      - name: event_timestamp
        tests:
          - not_null
      - name: event_name
        tests:
          - not_null
