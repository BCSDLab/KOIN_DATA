version: 2

sources:
  - name: app_db
    description: >
      앱 MySQL 회원 테이블을 Cloud Datastream으로 BigQuery에 복제한 원천.
      현재 물리 위치는 GA4 Export 데이터셋과 같지만 GA4 테이블은 아니다.
    database: kap-chat
    schema: analytics_432041405
    tables:
      - name: koin_users
        description: KOIN 앱 회원 원천 테이블
